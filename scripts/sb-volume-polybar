#!/bin/sh
# action=pavucontrol

Get() {
	volume=$(pamixer --get-volume)
	printf "%s%%" $volume

	if [ $(pamixer --get-mute) = "true" ]; then
		printf " (mute)"
	fi
}

echo %{u#ff5722}%{+u}Vol:$(Get)%{-u}
