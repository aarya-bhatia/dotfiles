XF86AudioRaiseVolume 	
	$HOME/scripts/volume.sh up

XF86AudioLowerVolume 	
	$HOME/scripts/volume.sh down

XF86AudioMute 			
	$HOME/scripts/volume.sh mute

XF86MonBrightnessUp 	
	$HOME/scripts/volume.sh brightness_up

XF86MonBrightnessDown 	
	$HOME/scripts/volume.sh brightness_down

XF86AudioPause			
	$HOME/scripts/volume.sh play_pause

XF86AudioPlay 			
	$HOME/scripts/volume.sh play_pause

XF86AudioNext 			
	$HOME/scripts/volume.sh next_track
	
XF86AudioPrev 			
	$HOME/scripts/volume.sh prev_track

# terminal emulator
alt + Return
	alacritty

# program launcher
alt + @d
	dmenu_run

# make sxhkd reload its configuration files:
alt + Escape
	pkill -USR1 -x sxhkd && notify-send "reloaded sxhkd"

alt + z
	notify-send "hello world"

# quit/restart bspwm
alt + shift + {x,r}
	bspc {quit,wm -r}

# close and kill
alt + {_,shift + }q
	bspc node -{c,k}

# switch to desktop
alt + {0-9}
	bspc desktop -f {0-9}

# alternate between the tiled and monocle layout
alt + m
	bspc desktop -l next

alt + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
	bspc desktop -f {prev,next}.local
